HEAD([example_frob], [frobnicate an example])
SYNO([int], [example_frob], [example_t, e], [const char*, method])
DESC([Frobnicates the example object e using the given method.])
RETV([Returns -1 on error or 0 on success])
ALSO([example_create, example_destroy])
FOOT
