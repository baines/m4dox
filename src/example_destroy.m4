HEAD([example_destroy], [destroys an example])
SYNO([void], [example_destroy], [example_t, e])
DESC([dnl
Destroys the example object e that was previously created with a
call to example_create.
])
ALSO([example_create, example_frob])
FOOT
